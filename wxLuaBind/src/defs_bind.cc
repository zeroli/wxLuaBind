#include <precompile.h>

#include "defs_bind.h"

REGISTER_WXLUA_BIND(defs)

int luaopen_defs(lua_State* L)
{
    BEGIN_LUA_TABLE(wx)
        BIND_TABLE_VALUE(NULL, nil)

        // enum: standard menu IDs
        BIND_ENUM(wxID_NONE)
        BIND_ENUM(wxID_SEPARATOR)
        BIND_ENUM(wxID_ANY)

        BIND_ENUM(wxID_LOWEST)
        BIND_ENUM(wxID_OPEN)
        BIND_ENUM(wxID_CLOSE)
        BIND_ENUM(wxID_NEW)
        BIND_ENUM(wxID_SAVE)
        BIND_ENUM(wxID_SAVEAS)
        BIND_ENUM(wxID_REVERT)
        BIND_ENUM(wxID_EXIT)
        BIND_ENUM(wxID_UNDO)
        BIND_ENUM(wxID_REDO)
        BIND_ENUM(wxID_HELP)
        BIND_ENUM(wxID_PRINT)
        BIND_ENUM(wxID_PRINT_SETUP)
        BIND_ENUM(wxID_PAGE_SETUP)
        BIND_ENUM(wxID_PREVIEW)
        BIND_ENUM(wxID_ABOUT)
        BIND_ENUM(wxID_HELP_CONTENTS)
        BIND_ENUM(wxID_HELP_INDEX)
        BIND_ENUM(wxID_HELP_SEARCH)
        BIND_ENUM(wxID_HELP_COMMANDS)
        BIND_ENUM(wxID_HELP_PROCEDURES)
        BIND_ENUM(wxID_HELP_CONTEXT)
        BIND_ENUM(wxID_CLOSE_ALL)
        BIND_ENUM(wxID_PREFERENCES)

        BIND_ENUM(wxID_EDIT)
        BIND_ENUM(wxID_CUT)
        BIND_ENUM(wxID_COPY)
        BIND_ENUM(wxID_PASTE)
        BIND_ENUM(wxID_CLEAR)
        BIND_ENUM(wxID_DUPLICATE)
        BIND_ENUM(wxID_SELECTALL)
        BIND_ENUM(wxID_DELETE)
        BIND_ENUM(wxID_REPLACE)
        BIND_ENUM(wxID_REPLACE_ALL)
        BIND_ENUM(wxID_PROPERTIES)

        BIND_ENUM(wxID_VIEW_DETAILS)
        BIND_ENUM(wxID_VIEW_LARGEICONS)
        BIND_ENUM(wxID_VIEW_SMALLICONS)
        BIND_ENUM(wxID_VIEW_LIST)
        BIND_ENUM(wxID_VIEW_SORTDATE)
        BIND_ENUM(wxID_VIEW_SORTNAME)
        BIND_ENUM(wxID_VIEW_SORTSIZE)
        BIND_ENUM(wxID_VIEW_SORTTYPE)

        BIND_ENUM(wxID_FILE)
        BIND_ENUM(wxID_FILE1)
        BIND_ENUM(wxID_FILE2)
        BIND_ENUM(wxID_FILE3)
        BIND_ENUM(wxID_FILE4)
        BIND_ENUM(wxID_FILE5)
        BIND_ENUM(wxID_FILE6)
        BIND_ENUM(wxID_FILE7)
        BIND_ENUM(wxID_FILE8)

        BIND_ENUM(wxID_OK)
        BIND_ENUM(wxID_CANCEL)
        BIND_ENUM(wxID_APPLY)
        BIND_ENUM(wxID_YES)
        BIND_ENUM(wxID_NO)
        BIND_ENUM(wxID_STATIC)
        BIND_ENUM(wxID_FORWARD)
        BIND_ENUM(wxID_BACKWARD)
        BIND_ENUM(wxID_DEFAULT)
        BIND_ENUM(wxID_MORE)
        BIND_ENUM(wxID_SETUP)
        BIND_ENUM(wxID_RESET)
        BIND_ENUM(wxID_CONTEXT_HELP)
        BIND_ENUM(wxID_YESTOALL)
        BIND_ENUM(wxID_NOTOALL)
        BIND_ENUM(wxID_ABORT)
        BIND_ENUM(wxID_RETRY)
        BIND_ENUM(wxID_IGNORE)
        BIND_ENUM(wxID_ADD)
        BIND_ENUM(wxID_REMOVE)

        BIND_ENUM(wxID_UP)
        BIND_ENUM(wxID_DOWN)
        BIND_ENUM(wxID_HOME)
        BIND_ENUM(wxID_REFRESH)
        BIND_ENUM(wxID_STOP)
        BIND_ENUM(wxID_INDEX)

        BIND_ENUM(wxID_BOLD)
        BIND_ENUM(wxID_ITALIC)
        BIND_ENUM(wxID_JUSTIFY_CENTER)
        BIND_ENUM(wxID_JUSTIFY_FILL)
        BIND_ENUM(wxID_JUSTIFY_RIGHT)
        BIND_ENUM(wxID_JUSTIFY_LEFT)
        BIND_ENUM(wxID_UNDERLINE)
        BIND_ENUM(wxID_INDENT)
        BIND_ENUM(wxID_UNINDENT)
        BIND_ENUM(wxID_ZOOM_100)
        BIND_ENUM(wxID_ZOOM_FIT)
        BIND_ENUM(wxID_ZOOM_IN)
        BIND_ENUM(wxID_ZOOM_OUT)
        BIND_ENUM(wxID_UNDELETE)
        BIND_ENUM(wxID_REVERT_TO_SAVED)

        BIND_ENUM(wxID_SYSTEM_MENU)
        BIND_ENUM(wxID_CLOSE_FRAME)
        BIND_ENUM(wxID_MOVE_FRAME)
        BIND_ENUM(wxID_RESIZE_FRAME)
        BIND_ENUM(wxID_MAXIMIZE_FRAME)
        BIND_ENUM(wxID_ICONIZE_FRAME)
        BIND_ENUM(wxID_RESTORE_FRAME)

        BIND_ENUM(wxID_FILEDLGG)

        BIND_ENUM(wxID_HIGHEST)

    END_LUA_TABLE(wx)
    return 0;
}

